require 'spec_helper'

describe Slzr::Chat do
  it 'has a version number' do
    expect(Slzr::Chat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
